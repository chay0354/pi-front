# Native projects generated by `expo prebuild` — EAS Build runs prebuild on the server for managed apps.
android/
ios/

node_modules/
.expo/
.expo-shared/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env.*
!.env.example
.DS_Store

# Local tarball caches — huge; never commit or upload in builds
react-native-*.tgz
